import { View, Text } from "react-native";
import React from "react";
import PlacesList from "../../components/Places/PlaceesList/PlacesList";

const AllPlaces = () => {
  return <PlacesList />;
};

export default AllPlaces;
